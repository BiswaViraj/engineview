{
  "name": "engineview",
  "version": "0.0.1",
  "description": "Self-hosted dashboard for Cloudflare Analytics Engine",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply engineview",
    "db:migrate:local": "wrangler d1 migrations apply engineview --local",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.78.0"
  }
}
